import bcrypt

# Define the password
password = b"admin"  # Note that bcrypt expects the password as bytes

# Generate a salt
salt = bcrypt.gensalt()

# Hash the password
hashed_password = bcrypt.hashpw(password, salt)

print(hashed_password)